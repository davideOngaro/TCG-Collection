spring:
  datasource:
    url: jdbc:postgresql://db:5432/onepiece
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

feign:
  client:
    config:
      tcg-api:
        url: https://apitcg.com/api/
        connectTimeout: 5000
        readTimeout: 10000